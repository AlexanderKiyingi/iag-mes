# iag-mes (Production)

Manufacturing execution / production microservice for the IAG platform.

Reserved scaffold — implementation will follow platform patterns used by `iag-quality-control` and `iag-warehouse` (gateway auth, Kafka events, shared notifications).

Registry: [`subrepos.json`](../../../subrepos.json) · Dev port: **4003**
